# Quick Task 260808-5az: ORB v2 geometry verification

**Goal:** Unify ORB pipeline in `OrbTemplateCamera` with RANSAC geometry, verification gates, and temporal state machine.

## Tasks

1. Merge `SingleTargetCamera` homography path into `OrbTemplateCamera`
2. Add mutual match, absolute Hamming, geometric validity (no confidence-only gate)
3. Add `DetectionState` SEARCHING/LOCKED/COASTING/LOST + movement authorization rules
4. Remove `SingleTargetCamera`; wire `OrbTarget1TestOpMode` to unified class
5. Update `CameraContinuationTest` policy constants

## Deferred (follow-up)

- Dynamic SEARCH/TRACK ROI
- One ORB extraction per physical webcam (orchestrator refactor)
- Manual exposure/gain/focus
