---
phase: 02-two-mode-opencv-camera
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - TeamCode/src/main/java/org/firstinspires/ftc/teamcode/core/TemplateMatchCamera.java
  - TeamCode/src/main/java/org/firstinspires/ftc/teamcode/opmode/TemplateMatchSingleTest.java
  - TeamCode/src/main/java/org/firstinspires/ftc/teamcode/opmode/TemplateMatchTest.java
autonomous: true
requirements: [VIS-01, VIS-02, VIS-03, VIS-06, VIS-07]
must_haves:
  truths:
    - Camera exposes explicit SINGLE_TARGET and MULTI_TARGET policy through one shared lifecycle.
    - SINGLE_TARGET publishes center, signed pixel errors, raw confidence, validity, and stale age for lateral centering.
    - Threshold, ROI/crop, temporal hold, NMS/min-distance, and center policy are named measurable configuration values.
    - Start, stop, invalid-template, invalid-frame, and camera-error paths remain safe and mode-independent.
  artifacts:
    - Shared mode/config/result contracts inside TemplateMatchCamera.java.
    - Updated single-target and multi-target OpModes showing mode and result telemetry.
  key_links:
    - OpMode reads CameraResult fields rather than recomputing freshness or center error.
    - Mode selection dispatches pipeline policy without duplicating webcam callbacks or release logic.
---

<objective>
Create shared two-mode camera contracts and preserve one safe lifecycle. Make SINGLE_TARGET output suitable for low-latency left-pallet centering, while exposing all measurable policies required by VIS-01, VIS-02, VIS-03, VIS-06, and VIS-07.

Purpose: Give later multi-target work a stable API and keep camera ownership, thread publication, and shutdown behavior in one class.
Output: Explicit mode/config/result APIs, shared pipeline dispatch, stale-age reporting, and mode-aware test OpModes.
</objective>

<execution_context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-two-mode-opencv-camera/02-RESEARCH.md
@TeamCode/src/main/java/org/firstinspires/ftc/teamcode/core/TemplateMatchCamera.java
@TeamCode/src/main/java/org/firstinspires/ftc/teamcode/opmode/TemplateMatchSingleTest.java
@TeamCode/src/main/java/org/firstinspires/ftc/teamcode/opmode/TemplateMatchTest.java
@TeamCode/src/main/java/org/firstinspires/ftc/teamcode/test/TemplateMatchCameraTest.java
</execution_context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Add shared mode, measurable config, and result contract</name>
  <files>TeamCode/src/main/java/org/firstinspires/ftc/teamcode/core/TemplateMatchCamera.java</files>
  <behavior>
    - Constructing each explicit mode uses the same TemplateMatchCamera lifecycle and pipeline.
    - A valid single detection publishes centerX/centerY, signed dxPx/dyPx, raw confidence, validity, timestamp, and staleAgeMs.
    - Empty or invalid input produces invalid results without OpenCV exceptions escaping the frame thread.
    - Public configuration exposes confidence threshold, frame scale/ROI, hold age, center deadband, NMS IoU, and minimum distance.
  </behavior>
  <action>Extend the existing constructor/API without adding camera classes. Add an explicit CameraMode with SINGLE_TARGET and MULTI_TARGET, an immutable measurable configuration using existing defaults where available, and an immutable CameraResult contract carrying single detection data, validity, timestamp, and staleAgeMs. Keep AtomicReference publication, reusable Mats, OpenCV TM_CCOEFF_NORMED, existing temporal filter behavior, and synchronized template replacement. Route processFrame through mode policy; SINGLE_TARGET must rank threshold-qualified candidates by center error with deterministic confidence/order tie-breaks, then apply existing filter/hold logic. Expose pure candidate-ranking and result/config contract helpers as public static APIs so the existing test package can call them without camera hardware. Validate null/blank asset names, empty frames, invalid template dimensions, and released/racing state. Keep raw confidence unchanged and do not steer inside camera code. Reset tracking when mode, target, or template registry changes per VIS-01, VIS-03, VIS-06, and VIS-07.</action>
  <verify><automated>gradlew.bat :TeamCode:compileDebugJavaWithJavac && java -ea -cp TeamCode\build\classes\java\main org.firstinspires.ftc.teamcode.test.TemplateMatchCameraTest</automated></verify>
  <done>Both modes compile through one lifecycle; SINGLE_TARGET exposes complete fresh/stale result data; named policies are readable from production API; invalid inputs and repeated shutdown remain safe.</done>
</task>

<task type="auto">
  <name>Task 2: Expose mode and stale telemetry in camera OpModes</name>
  <files>TeamCode/src/main/java/org/firstinspires/ftc/teamcode/opmode/TemplateMatchSingleTest.java, TeamCode/src/main/java/org/firstinspires/ftc/teamcode/opmode/TemplateMatchTest.java</files>
  <action>Update existing OpModes to select explicit mode through the shared constructor/config, display mode, target center, dxPx/dyPx, confidence, validity, staleAgeMs, policy values, camera state/error, and processing timing. Keep webcam1, existing target assets, edge-triggered controls, and finally camera.stop(). Do not duplicate detection/filter calculations or lifecycle handling in OpModes.</action>
  <verify><automated>gradlew.bat :TeamCode:compileDebugJavaWithJavac</automated></verify>
  <done>Both test OpModes compile and visibly distinguish mode, fresh versus stale result, signed center error, confidence, and camera lifecycle/error state.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Camera frame → OpenCV pipeline | Runtime image data can be empty, malformed, or produce pathological candidate counts. |
| Camera callback thread → OpMode thread | Results cross threads through shared state. |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-02-01 | Tampering | Template/frame inputs | medium | mitigate | Validate asset names, decoded dimensions, frame dimensions, and candidate bounds before matching. |
| T-02-02 | Denial of service | Candidate extraction/pipeline | high | mitigate | Bound candidate enumeration, reuse Mats, and avoid unbounded allocations in processFrame. |
| T-02-03 | Race condition | stop/setTarget/processFrame | high | mitigate | Guard running state, synchronize template replacement, publish immutable results atomically, release resources safely. |
| T-02-SC | Tampering | Dependency installs | high | accept | No packages are added; existing FTC/EasyOpenCV/OpenCV stack remains unchanged. |
</threat_model>

<verification>
- Run `gradlew.bat :TeamCode:compileDebugJavaWithJavac`.
- Run the executable `TemplateMatchCameraTest` using the repository's existing Java test invocation; all existing temporal tests and new contract assertions pass.
- Confirm source has one camera wrapper/lifecycle and explicit mode/config/result symbols.
</verification>

<success_criteria>
VIS-01, VIS-02, VIS-03, VIS-06, and VIS-07 are implemented through one shared camera lifecycle. Single-target callers can center from fresh signed error, reject stale results, and inspect every policy value without reconstructing hidden constants.
</success_criteria>

<output>
Create `.planning/phases/02-two-mode-opencv-camera/02-01-SUMMARY.md` when done.
</output>
