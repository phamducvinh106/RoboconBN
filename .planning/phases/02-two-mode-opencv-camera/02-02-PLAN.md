---
phase: 02-two-mode-opencv-camera
plan: 02
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - TeamCode/src/main/java/org/firstinspires/ftc/teamcode/core/TemplateMatchCamera.java
  - TeamCode/src/main/java/org/firstinspires/ftc/teamcode/test/TemplateMatchCameraTest.java
  - TeamCode/src/main/java/org/firstinspires/ftc/teamcode/opmode/TemplateMatchTest.java
autonomous: true
requirements: [VIS-04, VIS-05, VIS-06, VIS-07]
must_haves:
  truths:
    - MULTI_TARGET returns right and left block classifications before centering.
    - Threshold-qualified candidates become distinct detections after deterministic overlap/min-distance suppression.
    - Retained confidence stays raw match confidence and labels remain associated with configured templates.
    - Offline tests prove classification, thresholding, overlap suppression, stale behavior, and invalid/lifecycle safety.
  artifacts:
    - Multi-template registry and immutable list result in TemplateMatchCamera.java.
    - Pure package-visible candidate ranking/NMS helpers testable without camera hardware.
    - Extended executable TemplateMatchCameraTest.java coverage and multi-result OpMode telemetry.
  key_links:
    - Each configured template produces candidates that share one physical-box suppression policy.
    - Multi-result publication reaches OpMode before SINGLE_TARGET is selected for centering.
---

<objective>
Implement MULTI_TARGET candidate extraction, classification, and deterministic duplicate suppression, then prove both camera modes through executable offline tests and telemetry.

Purpose: Satisfy scan-pass classification without losing the shared lifecycle or raw confidence required for later autonomous pickup.
Output: Multi-template detection list, bounded threshold/NMS helpers, complete Phase 2 offline coverage, and scan telemetry.
</objective>

<execution_context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-two-mode-opencv-camera/02-RESEARCH.md
@TeamCode/src/main/java/org/firstinspires/ftc/teamcode/core/TemplateMatchCamera.java
@TeamCode/src/main/java/org/firstinspires/ftc/teamcode/test/TemplateMatchCameraTest.java
@TeamCode/src/main/java/org/firstinspires/ftc/teamcode/opmode/TemplateMatchTest.java
</execution_context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Add bounded multi-template candidates and suppression</name>
  <files>TeamCode/src/main/java/org/firstinspires/ftc/teamcode/core/TemplateMatchCamera.java, TeamCode/src/main/java/org/firstinspires/ftc/teamcode/test/TemplateMatchCameraTest.java</files>
  <behavior>
    - Candidates below configured confidence threshold are excluded.
    - Overlapping boxes or boxes within configured minimum center distance collapse deterministically.
    - Distinct left/right boxes remain, with label and raw confidence preserved.
    - Empty templates/frames, invalid boxes, repeated stop, and post-release frames remain safe.
  </behavior>
  <action>Extend the shared pipeline's MULTI_TARGET policy with an immutable configured template/label registry using current target.png through target4.png assets. For each valid template, match with TM_CCOEFF_NORMED, enumerate threshold-qualified peaks with bounded count and reusable response/mask Mats, map scaled coordinates to full-frame coordinates, create immutable candidates, and apply deterministic greedy suppression sorted by descending raw confidence then stable template/scan order. Suppress when configured IoU or center-distance policy matches; retain raw mm.maxVal without renormalization. Publish all retained detections in CameraResult while preserving SINGLE_TARGET's one selected detection and temporal hold semantics. Keep lifecycle guards and mode/template reset behavior from 02-01. Add pure public static geometry/ranking helpers callable from the existing test package so tests do not require webcam hardware.</action>
  <verify><automated>gradlew.bat :TeamCode:compileDebugJavaWithJavac</automated></verify>
  <done>MULTI_TARGET returns one labeled raw-confidence detection per distinct physical candidate, preserves both left/right blocks, bounds work per frame, and shares safe lifecycle with SINGLE_TARGET.</done>
</task>

<task type="auto">
  <name>Task 2: Extend executable vision tests and scan telemetry</name>
  <files>TeamCode/src/main/java/org/firstinspires/ftc/teamcode/test/TemplateMatchCameraTest.java, TeamCode/src/main/java/org/firstinspires/ftc/teamcode/opmode/TemplateMatchTest.java</files>
  <action>Extend the existing plain-Java executable test without copying production filter logic. Cover explicit mode contract, center-error ranking, result fields and stale-age expiry, threshold filtering, IoU/min-distance suppression, distinct two-label classification, raw confidence retention, invalid input, and reset/lifecycle guards. Update TemplateMatchTest to run MULTI_TARGET with configured target assets, display every retained label/box/confidence and measurable policy, and preserve existing target switching controls where compatible.</action>
  <verify><automated>gradlew.bat :TeamCode:compileDebugJavaWithJavac && java -ea -cp TeamCode\build\classes\java\main org.firstinspires.ftc.teamcode.test.TemplateMatchCameraTest</automated></verify>
  <done>Executable test exits zero with all VIS-01..VIS-07 assertions; OpMode telemetry demonstrates right/left classification before centering and reports unsuppressed raw confidence values.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Camera frame/template registry → candidate loop | Runtime data and asset dimensions drive matching and candidate count. |
| Candidate list → result publication | Detection objects cross from pipeline thread to OpMode thread. |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-02-03 | Tampering | Candidate geometry/confidence | medium | mitigate | Reject non-finite values, invalid bounds, and invalid template/frame dimensions before ranking. |
| T-02-04 | Denial of service | Multi-candidate extraction | high | mitigate | Bound peak enumeration, reuse OpenCV Mats, cap retained candidates, and use deterministic greedy suppression. |
| T-02-05 | Information disclosure | Result publication | low | accept | Results contain only local image coordinates, labels, and scores; no network or user data surface exists. |
| T-02-SC | Tampering | Dependency installs | high | accept | No packages are added; existing FTC/EasyOpenCV/OpenCV stack remains unchanged. |
</threat_model>

<verification>
- Run `gradlew.bat :TeamCode:compileDebugJavaWithJavac`.
- Run `java -ea -cp TeamCode\build\classes\java\main org.firstinspires.ftc.teamcode.test.TemplateMatchCameraTest` from repository root, adjusting classpath only if existing Gradle layout requires it.
- Confirm two retained synthetic candidates have distinct labels/locations and original raw scores; overlapping duplicates are absent.
- Confirm OpMode remains finally-safe on stop and reports camera errors without crashing.
</verification>

<success_criteria>
VIS-04 and VIS-05 are fully implemented; VIS-06 and VIS-07 remain true after multi-target expansion. Complete Phase 2 success criteria hold: multi scan classifies both blocks before centering, single mode tracks left center, duplicates collapse, raw confidence remains raw, and lifecycle/error behavior is identical across modes.
</success_criteria>

<output>
Create `.planning/phases/02-two-mode-opencv-camera/02-02-SUMMARY.md` when done.
</output>
