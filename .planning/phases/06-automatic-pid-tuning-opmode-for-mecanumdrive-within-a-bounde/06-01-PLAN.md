---
phase: 06-automatic-pid-tuning-opmode-for-mecanumdrive-within-a-bounde
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - TeamCode/src/main/java/org/firstinspires/ftc/teamcode/opmode/AutomaticPidTuningOpMode.java
autonomous: true
requirements:
  - PH6-01
  - PH6-02
  - PH6-03
  - PH6-04
  - PH6-05
user_setup: []
must_haves:
  truths:
    - "OpMode runs a deterministic matrix of many PID trials covering cardinal, diagonal, combined X/Y/heading targets, and positive and negative headings."
    - "Every commanded X/Y target is clamped inside an explicitly documented interior margin of the 50 x 50 cm square, and live boundary approach stops the drive safely."
    - "Each trial resets pose, applies runtime gains, updates Localizer before MecanumDrive, settles or times out, and records measurable results."
    - "Operator sees current trial, target, gains, clamp status, pose, errors, power, settle/timeout state, and best-so-far results through FTC telemetry only."
    - "OpMode changes no Localizer or MecanumDrive source and writes no files, preferences, or automatic gain constants."
  artifacts:
    - path: "TeamCode/src/main/java/org/firstinspires/ftc/teamcode/opmode/AutomaticPidTuningOpMode.java"
      provides: "Bounded automatic multi-trial PID tuning LinearOpMode"
      contains: "@TeleOp"
  key_links:
    - from: "AutomaticPidTuningOpMode"
      to: "Localizer and MecanumDrive"
      via: "RobotHardware initialization, resetPoseAndHeading, runtime gain setters, goToPosition, update"
    - from: "trial target generation"
      to: "MecanumDrive.goToPosition"
      via: "finite target clamp immediately before command issuance"
    - from: "trial loop"
      to: "FTC telemetry"
      via: "live metrics and in-memory result summary"
---

<objective>
Create one automatic FTC calibration OpMode that sweeps many bounded MecanumDrive PID trials and reports results without changing core control code.

Purpose: Give operator repeatable direction, combined-pose, heading, and gain comparisons while preserving existing Localizer/MecanumDrive contracts and telemetry-only output (D-01 through D-06).
Output: `AutomaticPidTuningOpMode.java` under `TeamCode/.../opmode/`.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-automatic-pid-tuning-opmode-for-mecanumdrive-within-a-bounde/06-CONTEXT.md
@.planning/phases/06-automatic-pid-tuning-opmode-for-mecanumdrive-within-a-bounde/06-RESEARCH.md
@.planning/phases/06-automatic-pid-tuning-opmode-for-mecanumdrive-within-a-bounde/06-PATTERNS.md
@TeamCode/src/main/java/org/firstinspires/ftc/teamcode/core/MecanumDrive.java
@TeamCode/src/main/java/org/firstinspires/ftc/teamcode/core/Localizer.java
@TeamCode/src/main/java/org/firstinspires/ftc/teamcode/core/RobotHardware.java
@TeamCode/src/main/java/org/firstinspires/ftc/teamcode/opmode/PidTuningOpMode.java
@TeamCode/src/main/java/org/firstinspires/ftc/teamcode/opmode/MecanumDriveGamepadTargetTestOpMode.java
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define bounded deterministic trial matrix</name>
  <files>TeamCode/src/main/java/org/firstinspires/ftc/teamcode/opmode/AutomaticPidTuningOpMode.java</files>
  <action>Create a single `@TeleOp` calibration `LinearOpMode` using existing `RobotHardware`, `Localizer`, and `MecanumDrive` APIs. Keep trial definitions and result holders private nested types in this file. Build a fixed matrix with multiple cardinal directions, diagonals, combined X/Y/heading targets, positive and negative headings, and several conservative gain candidates around existing defaults per D-01 and D-02. Use the project convention X=right, Y=forward, positive heading=counter-clockwise per D-05. Choose and document a safe interior margin within ±25 cm. Define measurable `LIVE_WARNING_THRESHOLD_CM` and `LIVE_ABORT_THRESHOLD_CM` constants from the reset origin, with warning strictly inside abort and abort strictly inside the physical ±25 cm boundary; publish both thresholds and live clearance in telemetry. Clamp every generated X/Y target again immediately before `goToPosition`, and retain unclamped/clamped values plus clamp status for telemetry per D-03 and D-04. During every active loop, warn at the live warning threshold and stop/abort at the live abort threshold before the physical boundary; record warning/abort state in the result while preserving telemetry-only output. Do not alter `MecanumDrive.java`, `Localizer.java`, `RobotHardware.java`, or add dependencies.
  <verify>
    <automated>./gradlew :TeamCode:compileDebugJavaWithJavac &amp;&amp; rg -n "localizer\.update\(\)|drive\.update\(\)|drive\.stop\(\)|goToPosition\(|telemetry\." TeamCode/src/main/java/org/firstinspires/ftc/teamcode/opmode/AutomaticPidTuningOpMode.java &amp;&amp; ! rg -n "File|SharedPreferences|FileOutputStream|OkHttp|HttpURLConnection" TeamCode/src/main/java/org/firstinspires/ftc/teamcode/opmode/AutomaticPidTuningOpMode.java</automated>
  </verify>
  <done>New OpMode compiles; matrix visibly contains cardinal, diagonal, combined pose, positive heading, and negative heading cases; every command path passes finite clamped X/Y values and preserves heading sign convention.</done>
</task>

<task type="auto">
  <name>Task 2: Run trials, score settling, and report telemetry</name>
  <files>TeamCode/src/main/java/org/firstinspires/ftc/teamcode/opmode/AutomaticPidTuningOpMode.java</files>
  <action>Implement lifecycle and one-trial-at-a-time execution. Before each trial stop drive, call `resetPoseAndHeading()`, apply position and heading gains through setters, allow sensor baseline update, then issue the clamped target. In each active loop call `localizer.update()` before `drive.update()`. Track elapsed time with monotonic timing, peak translational/heading error, final errors, settle qualification requiring consecutive in-tolerance loops, timeout, stop request, live warning threshold, and live abort threshold. On timeout, stop and record failure; on stop request or live abort threshold breach, stop immediately, record the boundary-abort reason and measured pose/clearance, and exit safely. Warning threshold crossing must remain telemetry-visible without waiting for physical-boundary contact. Use low power limits based on existing calibration patterns. Maintain only in-memory best score and a small rolling result summary; show gains, target, clamp, pose, errors, state, motor power, settle, timeout, and best result through telemetry only per D-06. Use `try/finally` to always call `drive.stop()`, and never write files or mutate core defaults.
  <verify>
    <automated>./gradlew :TeamCode:compileDebugJavaWithJavac &amp;&amp; rg -n "localizer\.update\(\)|drive\.update\(\)|drive\.stop\(\)|goToPosition\(|telemetry\." TeamCode/src/main/java/org/firstinspires/ftc/teamcode/opmode/AutomaticPidTuningOpMode.java &amp;&amp; ! rg -n "File|SharedPreferences|FileOutputStream|OkHttp|HttpURLConnection" TeamCode/src/main/java/org/firstinspires/ftc/teamcode/opmode/AutomaticPidTuningOpMode.java</automated>
  </verify>
  <done>Full matrix advances only after settle/timeout/abort; timeout, stop request, and boundary safety leave motors stopped; telemetry provides enough per-trial data for manual gain selection; no persistence or core-source diff exists.</done>
</task>

<task type="auto">
  <name>Task 3: Verify bounded and telemetry-only paths</name>
  <files>TeamCode/src/main/java/org/firstinspires/ftc/teamcode/opmode/AutomaticPidTuningOpMode.java</files>
  <action>Keep target clamping, heading normalization/scoring, and trial-matrix construction as small pure private methods. Validate finite generated targets and gains before command issuance. Add explicit static checks in the verification step for the ±25 cm outer bound, interior safe margin, `localizer.update()` before `drive.update()`, `drive.stop()` in `finally`, and absence of file/preferences/network APIs. Do not add a test framework, persistence, or core-source changes.</action>
  <verify>
    <automated>./gradlew :TeamCode:compileDebugJavaWithJavac &amp;&amp; rg -n "localizer\.update\(\)|drive\.update\(\)|drive\.stop\(\)|goToPosition\(|telemetry\." TeamCode/src/main/java/org/firstinspires/ftc/teamcode/opmode/AutomaticPidTuningOpMode.java &amp;&amp; ! rg -n "File|SharedPreferences|FileOutputStream|OkHttp|HttpURLConnection" TeamCode/src/main/java/org/firstinspires/ftc/teamcode/opmode/AutomaticPidTuningOpMode.java</automated>
  </verify>
  <done>Pure bounded behavior is independently inspectable or executable; no target can escape configured square bounds; no new dependency or test framework is added.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Operator/FTC lifecycle → OpMode | Stop requests and hardware state can interrupt trial execution. |
| OpMode → physical robot | Generated targets and motor commands can move hardware. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-06-01 | Tampering | Target matrix and clamp | high | mitigate | Clamp X/Y inside interior margin immediately before `goToPosition`; reject non-finite values. |
| T-06-02 | Denial of Service | Trial loop | high | mitigate | Enforce per-trial timeout, stop-request checks, short loop delays, and `finally` drive stop. |
| T-06-03 | Tampering | Heading targets | medium | mitigate | Preserve positive counter-clockwise/negative clockwise convention and use wrapped drive error API. |
| T-06-04 | Information disclosure | Tuning results | low | accept | Results remain transient FTC telemetry only; no network or persistence exists. |
| T-06-SC | Tampering | Project dependencies | low | accept | No package installs or dependency changes in this phase. |
</threat_model>

<verification>
Compile TeamCode. Run the checks in `06-VALIDATION.md`, recording command, result, build identity, and source evidence for matrix categories, command clamp, update order, shutdown, telemetry-only output, and forbidden APIs. Review diff to confirm only new OpMode is added and `Localizer`/`MecanumDrive` remain unchanged. On hardware, run the short low-power subset first, verify telemetry displays `LIVE_WARNING_THRESHOLD_CM`, `LIVE_ABORT_THRESHOLD_CM`, live pose/clearance, warning transition, boundary abort, motor-zero evidence, and emergency stop; then run full matrix while recording the validation artifact fields required by `06-VALIDATION.md`.
</verification>

<success_criteria>
One compiled OpMode runs many deterministic bounded trials across directions, combined targets, headings, and gain candidates; all safety exits stop drive; telemetry-only results identify settling, errors, timing, clamp status, and best candidate; no core changes or persistence.
</success_criteria>

<output>
Create `.planning/phases/06-automatic-pid-tuning-opmode-for-mecanumdrive-within-a-bounde/06-01-SUMMARY.md` when done.
</output>
