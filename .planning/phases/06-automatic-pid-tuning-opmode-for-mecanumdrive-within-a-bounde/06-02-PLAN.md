---
phase: 06-automatic-pid-tuning-opmode-for-mecanumdrive-within-a-bounde
plan: 02
type: execute
wave: 2
depends_on:
  - 06-01
files_modified:
  - TeamCode/src/main/java/org/firstinspires/ftc/teamcode/opmode/AutomaticPidTuningOpMode.java
autonomous: false
requirements:
  - PH6-06
must_haves:
  truths:
    - "Compiled OpMode runs on configured FTC hardware with named drive devices and low-power limits."
    - "Short subset and full matrix demonstrate safe stop on timeout, stop request, and live boundary approach."
    - "Operator can compare trials from telemetry without any generated file or automatic source constant change."
  artifacts:
    - path: "TeamCode/src/main/java/org/firstinspires/ftc/teamcode/opmode/AutomaticPidTuningOpMode.java"
      provides: "Deployable hardware calibration entry point"
      contains: "finally"
  key_links:
    - from: "FTC hardware configuration"
      to: "RobotHardware and MecanumDrive"
      via: "leftfront/rightfront/leftback/rightback and Localizer initialization"
    - from: "physical trial execution"
      to: "telemetry result ledger"
      via: "settle, timeout, clamp, and boundary metrics"
---

<objective>
Hardware-verify the automatic tuning OpMode and capture operator approval for bounded physical execution.

Purpose: Software compilation cannot prove motor directions, odometry signs, square clearance, or stop behavior. Human verification validates the safety-critical runtime contract after Plan 01 implementation.
Output: Approved hardware run or concrete issues for correction.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-automatic-pid-tuning-opmode-for-mecanumdrive-within-a-bounde/06-CONTEXT.md
@.planning/phases/06-automatic-pid-tuning-opmode-for-mecanumdrive-within-a-bounde/06-RESEARCH.md
@.planning/phases/06-automatic-pid-tuning-opmode-for-mecanumdrive-within-a-bounde/06-PATTERNS.md
@TeamCode/src/main/java/org/firstinspires/ftc/teamcode/opmode/AutomaticPidTuningOpMode.java
</context>

<tasks>

<task type="auto">
  <name>Task 1: Compile and inspect phase boundaries</name>
  <files>TeamCode/src/main/java/org/firstinspires/ftc/teamcode/opmode/AutomaticPidTuningOpMode.java</files>
  <action>Run the TeamCode compile task and inspect the final diff. Confirm only the planned OpMode changed, no dependency was added, no file/prefs/network output exists, and no `Localizer` or `MecanumDrive` source changed. Confirm the matrix and clamp paths visibly implement D-01 through D-06 before deploying.</action>
  <verify>
    <automated>./gradlew :TeamCode:compileDebugJavaWithJavac</automated>
  </verify>
  <done>Build passes and scope review confirms telemetry-only OpMode with no core changes or persistence.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify bounded hardware trial run</name>
  <what-built>Automatic PID tuning OpMode with deterministic multi-direction trial matrix, clamped 50 x 50 cm targets, low-power control, live boundary protection, settle/timeout scoring, and telemetry-only results.</what-built>
  <how-to-verify>
    1. Place robot at documented reset origin, physically clear the interior square, attach an operator spotter, and keep the controller emergency stop ready.
    2. Confirm configured devices include `leftfront`, `rightfront`, `leftback`, `rightback`, odometry/IMU hardware required by `RobotHardware`, and no obstruction near the 50 x 50 cm boundary.
    3. Deploy and select the new calibration OpMode. Before start, verify telemetry shows safe margin, low power, trial count, and telemetry-only output.
    4. Run a short subset first. Confirm robot moves only toward clamped targets, positive heading turns counter-clockwise, negative heading turns clockwise, and telemetry shows X/Y/heading errors and clamp status.
    5. Trigger or observe a stop request during a trial. Confirm motors stop immediately and OpMode exits without continuing motion.
    6. Allow at least one trial to settle and one to timeout if hardware behavior permits. Confirm each result records completion/timeout, elapsed time, final errors, and best-so-far score.
    7. During a supervised short-subset trial, observe the configured live warning threshold before the live abort threshold. Confirm telemetry records threshold values, live pose, clearance, warning state, abort reason, and motor power zero after `drive.stop()`; never intentionally cross the physical boundary.
    8. Run the full matrix only after short subset is safe. Confirm cardinal, diagonal, combined, and both heading-sign cases execute; no command target exceeds bounds; no files or automatic constants are created. Complete the `06-VALIDATION.md` run record with build identity, matrix IDs, thresholds, maximum live pose, closest boundary distance, all exit outcomes, telemetry evidence, and operator/spotter approval.
  </how-to-verify>
  <resume-signal>Type "approved" or describe hardware, boundary, heading, telemetry, or shutdown issues.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Human operator → physical robot | Operator controls deployment and emergency stop during movement. |
| OpMode → hardware | PID trial commands create physical motion inside constrained test area. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-06-05 | Denial of Service | Hardware trial | critical | mitigate | Require preflight, spotter, low-power subset, stop-request handling, timeout, and `finally` stop before full matrix. |
| T-06-06 | Tampering | Hardware configuration | high | mitigate | Verify exact configured device names and inspect direction/pose behavior before approving full run. |
| T-06-SC | Tampering | Project dependencies | low | accept | No package installs or dependency changes. |
</threat_model>

<verification>
Compile first, then perform blocking hardware checkpoint. Record any failed target clamp, wrong heading sign, boundary approach, timeout, or stop behavior as issues before approval.
</verification>

<success_criteria>
Operator approves short and full bounded runs: every trial remains within commanded square targets, safety exits stop motors, heading convention matches D-05, telemetry provides manual tuning evidence, and no persistence/core mutation occurs.
</success_criteria>

<output>
Create `.planning/phases/06-automatic-pid-tuning-opmode-for-mecanumdrive-within-a-bounde/06-02-SUMMARY.md` when done.
</output>
