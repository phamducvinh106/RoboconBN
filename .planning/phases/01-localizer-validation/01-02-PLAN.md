---
phase: 01-localizer-validation
plan: 02
type: execute
wave: 2
depends_on: [01-01]
files_modified:
  - TeamCode/src/main/java/org/firstinspires/ftc/teamcode/core/Localizer.java
  - TeamCode/src/main/java/org/firstinspires/ftc/teamcode/test/LocalizerMathTest.java
  - TeamCode/src/main/java/org/firstinspires/ftc/teamcode/opmode/OdometryCalibrationOpMode.java
autonomous: true
requirements: [LOC-04, LOC-05]
must_haves:
  truths:
    - Forward, strafe, rotation, and combined movement produce expected local/global signs and magnitudes at known headings.
    - Heading deltas wrap across +pi/-pi without a discontinuity.
    - Pose reset clears pose, deltas, and offset accumulators while resetPoseAndHeading also resets IMU yaw.
    - Offset suggestions are finite only after sufficient accumulated heading and match measured rotation fixtures.
  artifacts:
    - TeamCode/src/main/java/org/firstinspires/ftc/teamcode/test/LocalizerMathTest.java provides deterministic executable fixtures for update, transform, wrapping, reset, and offset suggestions.
    - TeamCode/src/main/java/org/firstinspires/ftc/teamcode/core/Localizer.java contains corrected, testable transform and angle-wrap behavior with calibration constants from 01-01.
    - TeamCode/src/main/java/org/firstinspires/ftc/teamcode/opmode/OdometryCalibrationOpMode.java displays primitive/combined transform pass-fail results and measured-vs-expected values.
  key_links:
    - Test fixtures feed encoder deltas and IMU yaw through the same Localizer update path, not a copied transform formula.
    - Localizer's local forward/left values map consistently to global X/Y at heading 0, +90, and -90 degrees.
    - Rotation measurements from 01-01 supply offset suggestions and do not get conflated with translation scale.
---

<objective>
Verify and correct Localizer integration, frame transform, heading wrap, reset behavior, and rotation offset suggestions.

Purpose: Prove the pose reported to MecanumDrive matches measured robot and field directions before later autonomous control depends on it.
Output: Deterministic math checks and hardware telemetry that expose any transform correction with measured evidence.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-localizer-validation/01-RESEARCH.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/TESTING.md
@TeamCode/src/main/java/org/firstinspires/ftc/teamcode/core/Localizer.java
@TeamCode/src/main/java/org/firstinspires/ftc/teamcode/opmode/OdometryCalibrationOpMode.java
@TeamCode/src/main/java/org/firstinspires/ftc/teamcode/core/MecanumDrive.java
@.planning/phases/01-localizer-validation/01-01-SUMMARY.md
</context>

<tasks>
<!-- completed 2026-08-06 -->
<task type="auto">
  <name>Task 1: Add deterministic Localizer math verification</name>
  <files>TeamCode/src/main/java/org/firstinspires/ftc/teamcode/test/LocalizerMathTest.java, TeamCode/src/main/java/org/firstinspires/ftc/teamcode/core/Localizer.java</files>
  <action>Per LOC-04 and LOC-05, create executable offline fixtures that compile without FTC SDK by extracting only the pure Localizer math into a package-private test model or deterministic adapter; do not claim an existing test-constructor seam. Verify zero baseline, tick-to-distance conversion, forward-only, strafe-only, rotation compensation, and combined movement at heading 0, +pi/2, and -pi/2; assert signs and magnitudes against explicit tolerances. Exercise angleWrap across the pi boundary, multiple updates, resetPose versus resetPoseAndHeading semantics, tick/heading deadbands, NaN heading fallback, and offset suggestion denominator guard. If fixtures demonstrate a transform/sign defect, correct only the minimum Localizer math and preserve the calibrated contract from 01-01; do not tune unrelated MecanumDrive PID or add dependencies.</action>
  <verify><automated>gradlew.bat :TeamCode:assembleDebug</automated><automated>javac -d .planning\phases\01-localizer-validation\build TeamCode\src\main\java\org\firstinspires\ftc\teamcode\test\LocalizerMathTest.java &amp;&amp; java -cp .planning\phases\01-localizer-validation\build org.firstinspires.ftc.teamcode.test.LocalizerMathTest</automated></verify>
  <done>Every LOC-04/LOC-05 math behavior has a reproducible executable assertion, including boundary and reset cases, and any correction is minimal and build-verified.</done>
</task>

<task type="auto">
  <name>Task 2: Verify transforms against controlled robot motion</name>
  <files>TeamCode/src/main/java/org/firstinspires/ftc/teamcode/opmode/OdometryCalibrationOpMode.java</files>
  <action>Extend the calibration OpMode's reporting to run low-speed, operator-supervised forward, strafe, rotation, and combined-motion checks at known starting headings. For each stage capture expected tape/field displacement beside Localizer X/Y/heading, local deltas, and error; include heading wrap and reset checks. Use measured scale/sign/offset values from 01-01, explicit tolerances declared in telemetry, timeout and stop-request handling, and unconditional stop cleanup. Treat any mismatch as a recorded failed stage requiring correction and rerun, never as a hidden constant tweak.</action>
  <verify><automated>gradlew.bat :TeamCode:assembleDebug</automated><automated>Static review confirms transform checks cover forward, strafe, rotation, combined motion, three heading regions, reset, and wrap.</automated></verify>
  <done>Hardware evidence confirms local/global pose signs and magnitudes within declared tolerances, or produces a reproducible failing stage tied to a specific Localizer correction.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Test fixture/adapters -> Localizer | Synthetic hardware readings exercise production pose logic. |
| OpMode/operator -> physical robot | Controlled motion can cause physical harm if transform or sign is wrong. |
| Localizer pose -> MecanumDrive | Incorrect pose feeds closed-loop drive targets. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-01-04 | Tampering | Localizer transform and signs | high | mitigate | Deterministic primitive/combined fixtures plus measured hardware comparisons before accepting corrections. |
| T-01-05 | Denial of service | Controlled transform OpMode | high | mitigate | Low power, per-stage timeout, stop request, frozen sensor check, and unconditional motor stop. |
| T-01-06 | Elevation of privilege | Test-only seams | medium | mitigate | Keep fake adapters package-scoped/test-only; production constructor remains hardware-bound and validated. |
| T-01-SC | Tampering | Build dependencies | high | mitigate | No package installation; use existing FTC SDK and Gradle build only. |
</threat_model>

<verification>
Run LocalizerMathTest and TeamCode assembly after implementation. On robot, run each primitive and combined stage at controlled headings, compare telemetry against tape/field measurements, and rerun after any correction until all declared tolerances pass.
</verification>

<success_criteria>
LOC-04 and LOC-05 are proven by deterministic checks plus controlled hardware evidence; Localizer pose behavior is safe for Phase 2 consumers; no unrelated source code changes are made.
</success_criteria>

<output>
Create `.planning/phases/01-localizer-validation/01-02-SUMMARY.md` when done.
</output>
