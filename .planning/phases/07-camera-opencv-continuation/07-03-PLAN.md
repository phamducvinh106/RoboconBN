---
phase: 07-camera-opencv-continuation
plan: 03
type: execute
wave: 3
depends_on: [07-02]
files_modified:
  - TeamCode/src/main/java/org/firstinspires/ftc/teamcode/core/SingleTargetCamera.java
  - TeamCode/src/main/java/org/firstinspires/ftc/teamcode/opmode/OrbTarget1TestOpMode.java
  - TeamCode/src/main/java/org/firstinspires/ftc/teamcode/test/CameraContinuationTest.java
  - .planning/phases/07-camera-opencv-continuation/07-VALIDATION.md
  - .planning/phases/07-camera-opencv-continuation/07-VERIFICATION.md
autonomous: false
requirements: [VIS-02, VIS-03, VIS-06, VIS-07, TEST-01]
must_haves:
  truths:
    - "D-02 and D-04: CameraContinuationTest invokes production scalar geometry and tracker seams for exact pass/reject reasons, transitions, numbers, and determinism without dependencies or copied policy."
    - "OrbTarget1TestOpMode keeps camera creation/start in INIT and reports compact raw-versus-filtered, quality, rejection, state, jitter, FPS, and processing aggregates without per-match output."
    - "Hardware acceptance measures at least 300 processed frames after warm-up for stationary target, lateral movement, and negative scene."
    - "Acceptance records filtered p95 jitter, accepted jumps, false locks, acquisition latency, movement lag, FPS, processing p50/p95/max, and sustained over-budget runs against explicit initial targets."
    - "Planning evidence reports hardware results honestly and names tuned values without changing any forbidden component."
  artifacts:
    - path: TeamCode/src/main/java/org/firstinspires/ftc/teamcode/test/CameraContinuationTest.java
      provides: Dependency-free deterministic production-policy sequence checks
    - path: TeamCode/src/main/java/org/firstinspires/ftc/teamcode/opmode/OrbTarget1TestOpMode.java
      provides: INIT-safe hardware telemetry, aggregates, and repeatable condition measurements
    - path: .planning/phases/07-camera-opencv-continuation/07-VALIDATION.md
      provides: Executable offline-first and hardware acceptance procedure
    - path: .planning/phases/07-camera-opencv-continuation/07-VERIFICATION.md
      provides: Honest requirement evidence, measured values, tuned defaults, and blockers
  key_links:
    - from: CameraContinuationTest
      to: SingleTargetCamera production policy
      via: package-visible scalar geometry and temporal methods, not duplicated formulas
    - from: SingleTargetCamera.Result
      to: OrbTarget1TestOpMode telemetry
      via: compact quality, state, timing, jitter, and rejection fields
    - from: offline compile and deterministic checks
      to: hardware acceptance
      via: blocking order before FTC OpMode run
---

<objective>
Prove the strengthened single-target ORB policy offline, expose evidence on hardware, and tune named defaults from repeatable measurements.

Purpose: Thresholds are starting values until deterministic policy checks and measured robot data show jitter, false-positive, acquisition, response, and processing behavior.
Output: Support-only telemetry/test changes plus current validation and verification evidence.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-camera-opencv-continuation/07-CONTEXT.md
@.planning/phases/07-camera-opencv-continuation/07-RESEARCH.md
@.planning/phases/07-camera-opencv-continuation/07-02-SUMMARY.md
@TeamCode/build.gradle
@TeamCode/src/main/java/org/firstinspires/ftc/teamcode/core/SingleTargetCamera.java
@TeamCode/src/main/java/org/firstinspires/ftc/teamcode/opmode/OrbTarget1TestOpMode.java
@TeamCode/src/main/java/org/firstinspires/ftc/teamcode/test/CameraContinuationTest.java
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Prove production policy and expose tuning telemetry</name>
  <files>TeamCode/src/main/java/org/firstinspires/ftc/teamcode/test/CameraContinuationTest.java, TeamCode/src/main/java/org/firstinspires/ftc/teamcode/opmode/OrbTarget1TestOpMode.java, .planning/phases/07-camera-opencv-continuation/07-VALIDATION.md</files>
  <behavior>
    - Geometry accepts broad low-error rectangle evidence and rejects clustered, non-convex, tiny, huge, off-frame, non-finite, short-edge, extreme-skew, and high-reprojection cases with exact reasons.
    - Acquisition locks on third coherent frame; fixed jitter sequence lowers filtered RMS while preserving bounded final error; monotonic 5 px/frame movement stays accepted with bounded lag.
    - One 120 px spike is rejected, two coherent relocated observations reinitialize only on second, and rejection counters are exact.
    - One/two misses hold without observation or timestamp refresh; exact time and third-miss boundaries invalidate; future/old clocks, stop/error, non-finite center, and over-budget processing reject.
    - Identical observation/timestamp sequences produce equal scalar results and rejection states.
  </behavior>
  <action>Per D-02 and D-04, replace stale constant/four-target checks with fixed-timestamp scalar fixtures that invoke `SingleTargetCamera` production geometry and tracker seams. Assert exact states, rejection reasons, counters, timestamp preservation, bounded numeric behavior, and determinism without copying policy or adding a dependency. Per D-02, D-03, and D-07, keep `SingleTargetCamera` construction and `startAsync()` in `init()` and retain `stop()` cleanup. Add compact telemetry groups for lifecycle/state/observed age; raw/filtered center and jitter; match/inlier/reprojection/area/coverage quality; rejection and temporal counters; processing p50/p95/max, over-budget streak/count, and FPS. Use fixed-size scalar aggregation without debug images or per-match logging. Update `07-VALIDATION.md` with exact offline command, three 300-frame hardware conditions, initial thresholds, and evidence fields.</action>
  <verify>
    <automated>.\gradlew.bat :TeamCode:compileDebugJavaWithJavac :TeamCode:cameraContinuationTest --offline</automated>
  </verify>
  <done>Offline policy checks pass through production seams; INIT-safe OpMode and validation contract expose complete compact evidence for hardware tuning.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Measure single-target hardware acceptance</name>
  <what-built>Qualified and temporally stabilized `SingleTargetCamera`, deterministic offline checks, and INIT-safe ORB tuning telemetry.</what-built>
  <how-to-verify>
    1. Confirm `.\gradlew.bat :TeamCode:compileDebugJavaWithJavac :TeamCode:cameraContinuationTest --offline` passes before installing/running the OpMode.
    2. Run `ORB Target 1 Test` with `webcam1` and `target1.png`; confirm camera opens during INIT, preview/telemetry updates, stop is safe, and no stale/error/held result authorizes movement.
    3. After warm-up, record at least 300 processed frames with target stationary and centered. Initial target: at least 95 percent qualified observations after acquisition, filtered radial jitter p95 at most 4 px, and no accepted center jump above 35 px.
    4. Record at least 300 processed frames while robot or target moves laterally at the bench speed. Initial target: acquisition latency is reported in frames and milliseconds, filtered lag p95 stays below 40 px, genuine motion is not treated as isolated outlier, and stale output never authorizes movement.
    5. Record at least 300 processed frames on a negative scene including repetitive texture if available. Initial target: zero LOCKED acquisitions and zero movement-authorized results.
    6. Across all conditions, record FPS and processing p50/p95/max. Initial target: processing p95 at most 100 ms and no sustained over-budget run longer than 3 frames.
  </how-to-verify>
  <resume-signal>Type `approved` with stationary, movement, negative-scene, FPS, and processing evidence; otherwise report failed metrics and measured values.</resume-signal>
  <files>TeamCode/src/main/java/org/firstinspires/ftc/teamcode/opmode/OrbTarget1TestOpMode.java, .planning/phases/07-camera-opencv-continuation/07-VERIFICATION.md</files>
  <action>Run the physical three-condition procedure after offline gates pass and return measured evidence for Task 3 tuning.</action>
  <verify><automated>.\gradlew.bat :TeamCode:compileDebugJavaWithJavac :TeamCode:cameraContinuationTest --offline</automated></verify>
  <done>Stationary, movement, and negative-scene measurements contain required sample counts and timing/accuracy metrics.</done>
</task>

<task type="auto">
  <name>Task 3: Apply evidence-based tuning and finalize verification</name>
  <files>TeamCode/src/main/java/org/firstinspires/ftc/teamcode/core/SingleTargetCamera.java, TeamCode/src/main/java/org/firstinspires/ftc/teamcode/test/CameraContinuationTest.java, .planning/phases/07-camera-opencv-continuation/07-VERIFICATION.md</files>
  <action>After the checkpoint, write all measured conditions into `07-VERIFICATION.md`. If any initial target failed, adjust only named `SingleTargetCamera` constants from evidence: tighten descriptor/coverage/reprojection/projected-geometry gates to eliminate false locks first, then recover positive recall; adjust acquisition/smoothing/outlier values from jitter and lag; reduce ORB features before weakening safety gates when processing exceeds budget. Update deterministic expected bounds only when a tuned constant intentionally changes the production contract. Do not add runtime configuration unless an existing matching project pattern already exists. Rerun offline checks and every failed hardware condition, then record final values, pass/fail, and blockers honestly.</action>
  <verify>
    <automated>.\gradlew.bat :TeamCode:compileDebugJavaWithJavac :TeamCode:cameraContinuationTest --offline</automated>
  </verify>
  <done>`07-VERIFICATION.md` contains actual metrics, final named defaults, rerun evidence, requirement status, and any remaining blocker; no forbidden file changed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Production policy to offline evidence | Tests can falsely pass if they duplicate policy or assert constants only. |
| Result telemetry to tuning decision | Verbose or incomplete telemetry can distort timing or hide rejection causes. |
| Hardware scene to movement acceptance | Positive-only tuning can accept repetitive negative texture or stale centers. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-07-09 | Tampering | Deterministic test policy | high | mitigate | Tests invoke production scalar seams and assert exact states/reasons/numbers. |
| T-07-10 | Repudiation | Hardware acceptance evidence | medium | mitigate | Record sample counts, conditions, metric aggregates, thresholds, final constants, and failures in validation/verification. |
| T-07-11 | Spoofing | Negative hardware scene | high | mitigate | Mandatory 300-frame negative condition with zero-lock acceptance target. |
| T-07-12 | Denial of service | Telemetry path | medium | mitigate | Fixed-size scalar aggregation and compact groups; no per-match output or debug-frame allocation. |
| T-07-SC | Tampering | Build/dependency inputs | high | mitigate | Existing Gradle task and pinned dependencies only; no install task. |
</threat_model>

<verification>
Pass compile and dependency-free tests, then complete blocking hardware measurements and reconcile `07-VERIFICATION.md` with actual values, tuned defaults, remaining blockers, and strict scope audit.
</verification>

<success_criteria>
Stationary jitter, false positives, acquisition latency, movement response, FPS, and processing latency are measured; initial targets pass or failed metrics remain explicit with evidence-based retuning steps.
</success_criteria>

<output>
Create `.planning/phases/07-camera-opencv-continuation/07-03-SUMMARY.md` when done.
</output>
