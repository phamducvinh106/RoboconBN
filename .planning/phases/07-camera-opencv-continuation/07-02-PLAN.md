---
phase: 07-camera-opencv-continuation
plan: 02
type: execute
wave: 2
depends_on: [07-01]
files_modified:
  - TeamCode/src/main/java/org/firstinspires/ftc/teamcode/test/CameraContinuationTest.java
  - .planning/phases/07-camera-opencv-continuation/07-VALIDATION.md
autonomous: true
requirements: [VIS-02, VIS-03, VIS-04, VIS-05, VIS-06, VIS-07, TEST-01]
must_haves:
  truths:
    - "Offline checks prove center math, mode policy, classification ranking, overlap suppression, confidence thresholds, and stale-result rejection without FTC hardware."
    - "The validation contract gives a repeatable compile/test path for Phase 7 camera behavior."
  artifacts:
    - path: "TeamCode/src/main/java/org/firstinspires/ftc/teamcode/test/CameraContinuationTest.java"
      provides: "Plain-Java executable camera contract tests"
    - path: ".planning/phases/07-camera-opencv-continuation/07-VALIDATION.md"
      provides: "Phase verification commands and acceptance checks"
  key_links:
    - from: "CameraContinuationTest"
      to: "ColorContourCamera policy helpers/result contract"
      via: "assertions over deterministic offline inputs"
---

<objective>
Prove Phase 7 camera behavior offline and lock validation around lifecycle, stale results, single-target center policy, and multi-target classification policy.

Purpose: Camera code can compile while silently regressing control safety or candidate selection. D-04 requires dependency-free executable checks; D-05 requires stale/lifecycle safety coverage.
Output: One runnable plain-Java test class and finalized validation contract.
</objective>

<execution_context>
@.planning/phases/07-camera-opencv-continuation/07-CONTEXT.md
@.planning/phases/07-camera-opencv-continuation/07-RESEARCH.md
@.planning/phases/07-camera-opencv-continuation/07-VALIDATION.md
@TeamCode/src/main/java/org/firstinspires/ftc/teamcode/core/ColorContourCamera.java
@TeamCode/src/main/java/org/firstinspires/ftc/teamcode/test/MultiTargetCameraTest.java
</execution_context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Add executable offline camera contract checks</name>
  <files>TeamCode/src/main/java/org/firstinspires/ftc/teamcode/test/CameraContinuationTest.java</files>
  <behavior>
    - SINGLE_TARGET center error is signed from frame center and only fresh valid results are movement-safe.
    - MULTI_TARGET ranks qualified candidates, preserves raw confidence, and suppresses overlapping duplicates.
    - Invalid/no-frame inputs produce no usable detection.
    - Results older than the configured age, and results after CLOSED/ERROR state, are rejected.
    - Repeated start/stop transitions do not create duplicate lifecycle effects in the testable policy helpers.
  </behavior>
  <action>Per D-04, follow existing main/assertion-style tests and avoid JUnit or new dependencies. Exercise deterministic pure helpers or extracted policy methods from ColorContourCamera; if a helper is package-private, place the test in the matching package or expose only the smallest existing contract needed. Include an executable main that throws AssertionError on failure and prints a concise pass result. Do not duplicate production filter logic in the test.</action>
  <verify><automated>.gradlew.bat :TeamCode:compileDebugJavaWithJavac && java -ea -cp TeamCode/build/classes/java/main org.firstinspires.ftc.teamcode.test.CameraContinuationTest</automated></verify>
  <done>Offline test exits zero and covers every TEST-01 camera item plus lifecycle/stale-result safety; failure identifies the violated behavior.</done>
</task>
<task type="auto">
  <name>Task 2: Finalize phase validation and source audit</name>
  <files>.planning/phases/07-camera-opencv-continuation/07-VALIDATION.md</files>
  <action>Record exact repository Gradle/test commands, expected pass conditions, and manual FTC checks for both configured webcams. Include a source audit mapping ROADMAP goal, VIS-01 through VIS-07, research constraints, and D-01 through D-05 to Plan 01 or Plan 02. Confirm out-of-scope items remain excluded and no implementation edits occur outside the planned TeamCode camera files.</action>
  <verify><automated>test -f .planning/phases/07-camera-opencv-continuation/07-01-PLAN.md && test -f .planning/phases/07-camera-opencv-continuation/07-02-PLAN.md && rg -n "VIS-0[1-7]|D-0[1-5]|SOURCE AUDIT|Gradle" .planning/phases/07-camera-opencv-continuation/07-VALIDATION.md</automated></verify>
  <done>Validation file contains executable commands, expected outcomes, manual hardware checks, and complete four-source coverage with no unplanned item.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Test input to vision policy | Synthetic frames/results represent untrusted detector data. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-07-04 | Tampering | Offline policy assertions | medium | mitigate | Assert invalid, stale, duplicate, and low-confidence cases explicitly so regressions fail before hardware use. |
| T-07-SC | Tampering | Gradle dependency changes | high | mitigate | Reuse existing dependencies; inspect dependency diff and require no new package install. |
</threat_model>

<verification>Compile TeamCode, run CameraContinuationTest with assertions, and verify validation source audit covers GOAL/REQ/RESEARCH/CONTEXT.</verification>

<success_criteria>Offline checks pass for all camera safety and selection contracts; validation maps every Phase 7 source item to an executable plan task.</success_criteria>

<output>Create .planning/phases/07-camera-opencv-continuation/07-02-SUMMARY.md when done.</output>
