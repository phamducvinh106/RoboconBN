---
phase: 07-camera-opencv-continuation
plan: 02
type: execute
wave: 2
depends_on: [07-01]
files_modified:
  - TeamCode/src/main/java/org/firstinspires/ftc/teamcode/core/SingleTargetCamera.java
autonomous: true
requirements: [VIS-02, VIS-03, VIS-06, VIS-07, TEST-01]
must_haves:
  truths:
    - "D-06: two coherent detections remain tentative and the third locks from median center; tentative output never authorizes movement."
    - "Small qualified motion is smoothed while real coherent motion remains responsive; one discontinuous spike cannot replace locked output and relocation requires two coherent outliers."
    - "Held output is marked unobserved, preserves last-observed timestamp, and expires at 120 ms or 3 misses without becoming fresh again."
    - "Closed, error, stale, non-finite, non-LOCKED, unobserved, and over-processing-budget results cannot authorize movement."
    - "D-03 and D-07: lifecycle behavior remains unchanged and temporal defaults stay named beside existing camera tuning constants pending hardware evidence."
  artifacts:
    - path: TeamCode/src/main/java/org/firstinspires/ftc/teamcode/core/SingleTargetCamera.java
      provides: Deterministic acquisition, adaptive smoothing, outlier confirmation, miss hold, freshness, and movement-authority policy
  key_links:
    - from: geometrically qualified raw observation
      to: temporal policy
      via: one package-visible production observation/state transition seam
    - from: temporal state
      to: immutable movement-facing Result
      via: filtered center, observed flag, stable/miss counts, timestamps, jitter, and rejection reason
    - from: latest Result
      to: fresh and movement authorization
      via: STREAMING, LOCKED, observed, finite, age, and processing-budget conjunction
---

<objective>
Turn qualified raw centers into stable, responsive, fail-closed movement-facing output.

Purpose: Geometry gates remove false matches, but current one-hit publication and hold-only behavior still pass center noise and isolated jumps directly to control.
Output: Deterministic temporal policy inside `SingleTargetCamera.java`, ready for direct offline sequence checks and hardware telemetry.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-camera-opencv-continuation/07-CONTEXT.md
@.planning/phases/07-camera-opencv-continuation/07-RESEARCH.md
@.planning/phases/07-camera-opencv-continuation/07-01-SUMMARY.md
@TeamCode/src/main/java/org/firstinspires/ftc/teamcode/core/SingleTargetCamera.java
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add coherent acquisition and adaptive smoothing</name>
  <files>TeamCode/src/main/java/org/firstinspires/ftc/teamcode/core/SingleTargetCamera.java</files>
  <behavior>
    - States are SEARCHING, TENTATIVE, LOCKED, and HOLDING; only observed LOCKED output can authorize movement.
    - Three qualified centers within 18 px acquire lock at median X/Y; incoherent acquisition restarts from current observation.
    - Locked observations use alpha clamped to 0.12–0.70 from motion distance over 30 px and quality, with lower quality contributing less.
    - Recent jitter is median of the last five accepted raw-to-filter residuals; control-facing dx/dy gets 2 px deadband without altering tracked center telemetry.
  </behavior>
  <action>Per D-02, D-04, D-06, and D-07, add one package-visible deterministic tracker helper inside `SingleTargetCamera` that accepts scalar qualified observations and explicit timestamps. Keep all transition logic in this production helper so later tests do not copy it. Require three coherent observations for lock and initialize with component medians. Apply the research preferred adaptive EMA: motion increases response, quality increases accepted influence, alpha stays within named bounds. Maintain raw and filtered centers separately, calculate raw/filtered jitter metrics, and apply deadband only when deriving control-facing offsets. Reset tracker on template change, start generation change, stop, and camera error without changing camera INIT startup flow.</action>
  <verify>
    <automated>.\gradlew.bat :TeamCode:compileDebugJavaWithJavac --offline</automated>
  </verify>
  <done>SingleTargetCamera exposes deterministic SEARCHING/TENTATIVE/LOCKED transitions and measurably smoother centers without hiding raw movement.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Confirm outliers and bound miss/stale behavior</name>
  <files>TeamCode/src/main/java/org/firstinspires/ftc/teamcode/core/SingleTargetCamera.java</files>
  <behavior>
    - Continuity radius is 24 px plus 2.5 times recent jitter, capped at 60 px.
    - One outlier preserves locked filtered output and observation timestamp; two qualified outliers within 18 px relocate at their median.
    - Misses enter HOLDING for display continuity but never claim observation; hold ends at 120 ms or the third consecutive miss, whichever occurs first.
    - Observation age limit is 250 ms; future timestamps, non-finite centers, camera state outside STREAMING, processing above 100 ms, and any state except observed LOCKED reject movement.
  </behavior>
  <action>Per D-03 and D-06, run continuity checks only after frame-local qualification. Track one pending outlier and accept relocation only after a second coherent outlier; rejected spikes increment metrics and do not refresh observation time. On miss, retain filtered display center only while both time and miss-count bounds pass, set state HOLDING and `observedThisFrame=false`, and keep `lastObservedTimestampMs`. Publish SEARCHING invalid output at either boundary. Expand immutable `Result` only with fields required by the research result/metrics contract: state, observation flag/counts/time/age, raw and filtered centers/errors, jitter, quality components, rejection reason, processing/fps, and counters. Make `fresh` and movement authorization enforce lifecycle, state, observation, finite, age, confidence/hard-gate, and processing conditions together.</action>
  <verify>
    <automated>.\gradlew.bat :TeamCode:compileDebugJavaWithJavac --offline</automated>
  </verify>
  <done>Isolated jumps, misses, stale clocks, lifecycle errors, and over-budget frames cannot refresh or authorize output; coherent relocation remains possible after confirmation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Qualified frame observation to tracker | A valid-looking single-frame jump can still be physical noise or a false relocation. |
| Held display output to movement control | Repeated publication can replay old center data as apparently fresh. |
| Wall-clock and processing metrics to freshness | Future, over-age, or over-budget samples can bypass safety if predicates are incomplete. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-07-05 | Spoofing | Acquisition and relocation | high | mitigate | Three-frame coherent acquisition and two-frame coherent outlier relocation in production tracker. |
| T-07-06 | Replay | Miss hold and timestamps | high | mitigate | Preserve last-observed time, mark held output unobserved, and expire by time or miss count. |
| T-07-07 | Tampering | Movement authorization predicate | high | mitigate | Conjoin lifecycle, state, observation, finite, age, hard qualification, and processing budget. |
| T-07-08 | Denial of service | Tracker history | low | accept | Fixed five-sample scalar history has constant memory and bounded sort cost. |
| T-07-SC | Tampering | Build/dependency inputs | high | mitigate | Existing pinned stack and Java arrays only; no install task. |
</threat_model>

<verification>
Compile offline and inspect every temporal transition for deterministic timestamp behavior, bounded state, and one authoritative movement predicate.
</verification>

<success_criteria>
SingleTargetCamera suppresses stationary jitter and single spikes, acquires and relocates coherently, responds to real motion, and fails closed across misses, stale data, errors, and latency overruns.
</success_criteria>

<output>
Create `.planning/phases/07-camera-opencv-continuation/07-02-SUMMARY.md` when done.
</output>
