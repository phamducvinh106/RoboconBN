---
phase: 07-camera-opencv-continuation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - TeamCode/src/main/java/org/firstinspires/ftc/teamcode/core/SingleTargetCamera.java
autonomous: true
requirements: [VIS-02, VIS-03, VIS-06, VIS-07]
must_haves:
  truths:
    - "D-01 and D-05: only SingleTargetCamera production behavior changes, and weak, duplicate, clustered, or geometrically implausible ORB evidence cannot publish a target center."
    - "A qualified result reports transformed template center plus descriptor, inlier, reprojection, coverage, area, and rejection evidence instead of treating inlier ratio alone as confidence."
    - "D-03: camera startup, stop, error, late-callback, stale-result, and native-resource behavior remain fail closed."
    - "D-07: research thresholds begin as named defaults beside existing camera tuning constants and are not externalized without an existing project pattern."
  artifacts:
    - path: TeamCode/src/main/java/org/firstinspires/ftc/teamcode/core/SingleTargetCamera.java
      provides: Bounded ORB match qualification, homography validation, structured quality metrics, and lifecycle-safe immutable results
  key_links:
    - from: SingleTargetCamera.Pipeline ORB matches
      to: homography estimation
      via: sorted absolute-plus-ratio filtered unique correspondences with template spatial coverage
    - from: homography and RANSAC mask
      to: SingleTargetCamera.Result raw center
      via: finite inlier residual and projected quadrilateral gates before transformed template-center publication
    - from: camera lifecycle state
      to: movement-facing freshness
      via: STREAMING generation, finite values, observation timestamp, and processing-budget checks
---

<objective>
Harden `SingleTargetCamera` frame-local ORB evidence and geometry so noisy hardware matches fail before center publication.

Purpose: Current hardware output jumps because weak correspondence clusters and implausible homographies can pass an inlier-ratio-only gate.
Output: One lifecycle-preserving `SingleTargetCamera.java` with deterministic match qualification, geometry validation, quality metrics, and rejection reasons.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-camera-opencv-continuation/07-CONTEXT.md
@.planning/phases/07-camera-opencv-continuation/07-RESEARCH.md
@TeamCode/src/main/java/org/firstinspires/ftc/teamcode/core/SingleTargetCamera.java
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Qualify descriptor correspondences before homography</name>
  <files>TeamCode/src/main/java/org/firstinspires/ftc/teamcode/core/SingleTargetCamera.java</files>
  <behavior>
    - At least 12 retained matches are required after Hamming distance at most 64 and Lowe ratio below 0.72.
    - Passing matches are ordered deterministically by distance then indices, use one scene keypoint each, and cap retained work at 80.
    - Template match spread covers at least 15 percent of template area and three quadrants before RANSAC; clustered correspondences reject with a named reason.
    - ORB uses explicit bounded parameters with 400 features, 1.2 scale, 6 levels, HARRIS score, and fast threshold 15 unless existing source already has measured tighter values.
  </behavior>
  <action>Per D-01, D-05, and D-07, recover the last repository `SingleTargetCamera.java` baseline first if current checkout lacks it, then modify only this file. Keep one immutable template descriptor set prepared once. Make ORB constructor and match limits named constants near class top. Apply cheap gates in order: descriptor availability, KNN pair shape, absolute Hamming ceiling plus ratio, deterministic sorting, unique `trainIdx`, cap, count, template bounding-box coverage, and represented quadrants. Return a structured first-failure `RejectionReason` and scalar match metrics for every processed observation. Preserve bounded ROI and avoid adding reverse matching, dependencies, alternate camera classes, or unrelated optimization. Build package-visible pure scalar helpers only where `CameraContinuationTest` can later invoke the exact production policy.</action>
  <verify>
    <automated>.\gradlew.bat :TeamCode:compileDebugJavaWithJavac --offline</automated>
  </verify>
  <done>Only deterministically sorted, unique, spatially broad, bounded-quality correspondences reach homography; rejected frames expose exact match-stage reason and metrics.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Validate homography residuals and projected target geometry</name>
  <files>TeamCode/src/main/java/org/firstinspires/ftc/teamcode/core/SingleTargetCamera.java</files>
  <behavior>
    - Homography must be non-empty 3x3 with finite coefficients, at least 10 inliers, and inlier ratio at least 0.60.
    - Inlier median reprojection is at most 3 px, p90 at most 5 px, inlier template coverage at least 12 percent, and three quadrants represented.
    - Projected corners are finite, convex, consistently wound, within a 10 percent frame margin, and have area ratio 1.5–60 percent, edges at least 12 px, edge ratio at most 6, and diagonal ratio at most 4.
    - Published raw center is the perspective transform of template center, and composite confidence cannot override a failed hard gate.
  </behavior>
  <action>Per D-05, calculate residuals only after RANSAC, using its mask and installed OpenCV perspective transform. Add plain-Java shoelace, cross-product, finite, bounds, edge, diagonal, percentile, and coverage helpers inside `SingleTargetCamera`; keep them package-visible where deterministic tests need the production seam. Transform template center directly. Publish raw center, inlier count/ratio, median Hamming, median/p90 reprojection, projected area ratio, template coverage ratio, bounded composite quality using research weights, and first rejection reason. Enforce processing budget as movement-authority input, but report it as processing latency rather than capture latency. Preserve D-03 lifecycle generation, INIT opening behavior, idempotent stop, late-callback invalidation, and complete Mat/keypoint/matcher release paths.</action>
  <verify>
    <automated>.\gradlew.bat :TeamCode:compileDebugJavaWithJavac --offline</automated>
  </verify>
  <done>Only finite, low-error, spatially supported, plausible projected targets publish transformed centers; lifecycle and resource safety remain intact.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Camera frame to ORB evidence | Untrusted image texture can generate ambiguous or duplicate correspondences. |
| Homography output to target center | Degenerate transforms can produce finite-looking but unsafe remote centers. |
| Native camera callback to result publication | Stop/error races can publish after resources or lifecycle generation become invalid. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-07-01 | Spoofing | Descriptor correspondence path | high | mitigate | Absolute and ratio quality, uniqueness, spread, inlier, residual, and geometry gates in `SingleTargetCamera`. |
| T-07-02 | Tampering | Homography-to-center conversion | high | mitigate | Finite transform, reprojection, convexity, area, bounds, edge, and diagonal checks before transformed-center publication. |
| T-07-03 | Denial of service | Per-frame ORB work | medium | mitigate | Bounded ROI, explicit ORB limits, retained-match cap, and cheap rejection order. |
| T-07-04 | Replay | Async result publication | high | mitigate | Lifecycle generation and state checks invalidate late, stopped, errored, and stale results. |
| T-07-SC | Tampering | Build/dependency inputs | high | mitigate | Existing pinned FTC/OpenCV stack only; no install task. |
</threat_model>

<verification>
Compile offline, inspect changed path is only `SingleTargetCamera.java`, and confirm every result path either carries qualified metrics or an explicit rejection reason without weakening lifecycle gates.
</verification>

<success_criteria>
No weak or implausible ORB geometry can become a usable raw center; qualified output is measurable and ready for temporal stabilization.
</success_criteria>

<output>
Create `.planning/phases/07-camera-opencv-continuation/07-01-SUMMARY.md` when done.
</output>
