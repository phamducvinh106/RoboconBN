---
phase: 07-camera-opencv-continuation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - TeamCode/src/main/java/org/firstinspires/ftc/teamcode/core/ColorContourCamera.java
  - TeamCode/src/main/java/org/firstinspires/ftc/teamcode/core/RobotHardware.java
  - TeamCode/src/main/java/org/firstinspires/ftc/teamcode/opmode/LeftCameraCenteringTestOpMode.java
  - TeamCode/src/main/java/org/firstinspires/ftc/teamcode/opmode/RightCameraClassificationTestOpMode.java
autonomous: true
requirements: [VIS-01, VIS-02, VIS-03, VIS-04, VIS-05, VIS-06, VIS-07]
must_haves:
  truths:
    - "Both camera policies use one shared ColorContourCamera lifecycle and result contract."
    - "webcam1 is explicitly bound to SINGLE_TARGET centering and webcam2 is explicitly bound to MULTI_TARGET classification."
    - "Closed, failed, or stale camera results cannot be treated as movement-safe detections."
  artifacts:
    - path: "TeamCode/src/main/java/org/firstinspires/ftc/teamcode/core/ColorContourCamera.java"
      provides: "Shared mode/configuration, lifecycle, result freshness, and OpenCV pipeline"
    - path: "TeamCode/src/main/java/org/firstinspires/ftc/teamcode/core/RobotHardware.java"
      provides: "Explicit webcam1 and webcam2 hardware contract"
  key_links:
    - from: "LeftCameraCenteringTestOpMode"
      to: "ColorContourCamera"
      via: "webcam1 plus SINGLE_TARGET result freshness and dxPx"
    - from: "RightCameraClassificationTestOpMode"
      to: "ColorContourCamera"
      via: "webcam2 plus MULTI_TARGET classification result"
---

<objective>
Make ColorContourCamera the explicit Phase 7 shared camera contract, with separate single-target centering and multi-target classification policies, explicit webcam assignment, and lifecycle/stale-result safety.

Purpose: Current code has working ColorContourCamera behavior but requirement names, webcam ownership, and callback/result safety are not yet an explicit contract. D-01 through D-05 govern this plan.
Output: Updated camera core, RobotHardware webcam mapping, and both camera test OpModes; no new dependency and no TemplateMatchCamera.java.
</objective>

<execution_context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-camera-opencv-continuation/07-CONTEXT.md
@.planning/phases/07-camera-opencv-continuation/07-RESEARCH.md
@.planning/phases/07-camera-opencv-continuation/07-PATTERNS.md
@TeamCode/src/main/java/org/firstinspires/ftc/teamcode/core/ColorContourCamera.java
@TeamCode/src/main/java/org/firstinspires/ftc/teamcode/core/RobotHardware.java
</execution_context>

<tasks>
<task type="auto">
  <name>Task 1: Normalize shared camera mode and result contract</name>
  <files>TeamCode/src/main/java/org/firstinspires/ftc/teamcode/core/ColorContourCamera.java</files>
  <action>Per D-01 and D-02, retain ColorContourCamera as sole lifecycle owner and expose explicit SINGLE_TARGET and MULTI_TARGET semantics, using compatibility aliases only if existing callers require them. Keep mode-specific scoring: single target prioritizes left-pallet center error and multi target preserves qualified candidate classification, confidence, and overlap suppression. Make measurable thresholds/configuration visible through the existing config/result API. Guard async callbacks and result publication with lifecycle state/generation so startAsync and stop are idempotent, open/start failures enter ERROR, released pipeline resources are not reused, and getLatestResult can be rejected by state/error/freshness checks. Do not add TemplateMatchCamera.java or dependencies.</action>
  <verify><automated>MISSING — Wave 2 must create TeamCode/src/main/java/org/firstinspires/ftc/teamcode/test/CameraContinuationTest.java first</automated></verify>
  <done>One ColorContourCamera supports both explicit policies; result contains center/error/confidence/validity/timestamp and lifecycle-safe consumers can distinguish fresh streaming data from stale/closed/error data.</done>
</task>
<task type="auto">
  <name>Task 2: Bind both webcams and update OpMode consumers</name>
  <files>TeamCode/src/main/java/org/firstinspires/ftc/teamcode/core/RobotHardware.java, TeamCode/src/main/java/org/firstinspires/ftc/teamcode/opmode/LeftCameraCenteringTestOpMode.java, TeamCode/src/main/java/org/firstinspires/ftc/teamcode/opmode/RightCameraClassificationTestOpMode.java</files>
  <action>Per D-03 and D-05, add explicit webcam2 mapping beside webcam1 in RobotHardware without fallback. Update left OpMode to use webcam1 SINGLE_TARGET and stop drive whenever camera state is not streaming or result exceeds freshness limit. Update right OpMode to use webcam2 MULTI_TARGET and show classification, confidence, timestamp age, state, and error. Preserve try/finally cleanup and existing FTC device names.</action>
  <verify><automated>.gradlew.bat :TeamCode:compileDebugJavaWithJavac</automated><automated>.\gradlew.bat :TeamCode:compileDebugJavaWithJavac</automated></verify>
  <done>TeamCode compiles; source explicitly maps webcam1/webcam2; left OpMode cannot strafe on stale/error data; right OpMode reports webcam2 classification safely.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Camera callback to OpMode | Asynchronous pipeline data crosses into control logic. |
| HardwareMap to camera core | Configured device names select physical webcams. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-07-01 | Tampering | Camera result handoff | high | mitigate | Lifecycle generation, atomic result publication, and stale/error rejection prevent old frames driving motion. |
| T-07-02 | Denial of service | Camera open/stop | medium | mitigate | Idempotent lifecycle, guarded close, explicit ERROR state, and cleanup in finally. |
| T-07-03 | Tampering | Webcam selection | medium | mitigate | Explicit webcam1/webcam2 HardwareMap fields; no alias fallback. |
</threat_model>

<verification>Run Gradle TeamCode compilation and Wave 2 offline camera assertions; inspect no new dependency and no TemplateMatchCamera.java.</verification>

<success_criteria>Shared camera lifecycle compiles, both webcams are explicit, both policies expose required results, and stale/error results never command movement.</success_criteria>

<output>Create .planning/phases/07-camera-opencv-continuation/07-01-SUMMARY.md when done.</output>
